# Synthetic workspaces

These two fictional profiles are reference inputs for isolation and adapter tests. They demonstrate that candidate identity, locale, time zone, goals, and preferences differ between deployments.

They are not imported automatically. Enter one through onboarding only when you intentionally want synthetic test data. Use a separate database, environment file, credentials, worker registration, and schedule for each profile. A fresh production setup should contain neither example.
